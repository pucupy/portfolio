{
  "$schema": "./contract.schema.json",
  "id": "yui.banner",
  "name": "Banner",
  "version": "1.0.0",
  "status": "extracted",
  "source": {
    "file": "yapilyui.css",
    "repo": "yapily-brand/webroot",
    "lines": "2867–2890",
    "note": "Values transcribed from the shipped stylesheet, not authored for this contract."
  },
  "description": "A full-width centred notice on the alternative brand yellow, with the same 1px border as everything else. Dismissible via a close control that does not change colour on hover.",
  "semantics": {
    "element": "div",
    "role": "status"
  },
  "props": [
    {
      "name": "children",
      "type": "text",
      "default": "Sandbox data only.",
      "bindings": {
        "figma": {
          "kind": "TEXT",
          "property": "Message"
        },
        "code": {
          "prop": "children"
        }
      }
    },
    {
      "name": "dismissible",
      "type": "boolean",
      "default": true,
      "bindings": {
        "figma": {
          "kind": "BOOLEAN",
          "property": "Dismissible"
        },
        "code": {
          "prop": "dismissible"
        }
      }
    }
  ],
  "states": [],
  "anatomy": {
    "root": {
      "tokens": {
        "padding": "{space.inset.compact}",
        "background-color": "{color.surface.banner}",
        "color": "{color.text.primary}",
        "border": "{border.width.default} solid {color.border.default}",
        "text-align": "center"
      },
      "parts": {
        "message": {
          "element": "span",
          "content": {
            "prop": "children"
          }
        },
        "close": {
          "element": "button",
          "optional": true,
          "tokens": {
            "color": "{color.text.primary}"
          },
          "$note": ".closebtn:hover sets the same colour it already has — the hover rule is inert in the source."
        }
      }
    }
  },
  "a11y": {
    "contrast": "AA",
    "notes": "Black on #FEF1BF passes comfortably. The close control has no accessible name in the source."
  },
  "anchors": {
    "css": {
      "file": "yapilyui.css",
      "selectors": [
        ".alert",
        ".closebtn"
      ]
    }
  }
}
