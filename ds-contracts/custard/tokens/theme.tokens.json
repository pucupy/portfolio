{
  "$description": "The whole customer-facing theme contract: four values and a logo. Widening this file is how a white-label system stops being one. Everything else — spacing, type scale, structure, the 44px targets — is fixed and lives in primitives/semantic.",
  "theme": {
    "color-action-primary": {
      "$type": "color",
      "$value": "{color.ink}",
      "$description": "Primary button, focus ring, selected row tint."
    },
    "color-text-inverse": {
      "$type": "color",
      "$value": "{color.white}",
      "$description": "Label colour on the primary button."
    },
    "color-bg-canvas": {
      "$type": "color",
      "$value": "{color.canvas}",
      "$description": "Page ground behind the modal."
    },
    "layout-modal-radius": {
      "$type": "dimension",
      "$value": "{radius.600}",
      "$description": "Modal shell. 20px at ≤1199, 0 at ≤599."
    }
  }
}
