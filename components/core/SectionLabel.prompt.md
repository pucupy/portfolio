SectionLabel — small uppercase monospace eyebrow used above every section ("Experience", "Awards", "Featured").

```jsx
<SectionLabel>Experience</SectionLabel>
<SectionLabel as="h3">Awards</SectionLabel>
```

Text-secondary color, 13px, wide letter-spacing — never the sole way information is grouped (a hairline divider or spacing gap always accompanies it).
