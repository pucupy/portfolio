The only bordered container in the system, and the only place gold is used as a border. Closes a case study.

```jsx
<OutcomesCallout lead="Shipped to every Yapily customer in 2023.">
  <StatGrid variant="spaced" stats={stats} />
</OutcomesCallout>
```

Square corners, no background fill, no shadow. One per page at most.
