Numbers without chrome. The hairline variant fakes dividers with a 1px grid gap over a light background — there are no card borders anywhere else on the site.

```jsx
<StatGrid stats={[{ value: 'AA', label: 'Contrast target across every flow' }]} />
<StatGrid variant="spaced" stats={outcomes} />
```

Corners stay square. Never add a radius or shadow here.
