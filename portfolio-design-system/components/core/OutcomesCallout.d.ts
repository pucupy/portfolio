export interface OutcomesCalloutProps {
  heading?: React.ReactNode;
  /** 18px lead in --ink-1. */
  lead?: React.ReactNode;
  /** Usually a spaced StatGrid, or nothing. */
  children?: React.ReactNode;
}
/**
 * @startingPoint section="Core" subtitle="Gold-bordered outcomes block" viewport="700x260"
 */
export function OutcomesCallout(props: OutcomesCalloutProps): JSX.Element;
